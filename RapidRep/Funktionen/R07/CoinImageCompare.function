/////////----- Checks if CoinImage has the value that the cointype mostly is associated with, in the database. -----/////////


if ($ReferenceCoinImage == $CoinImage){ 
  return false;
}
  else {
    return true;
  }
 
