/////////----- Checks if Culture has the value that the cointype mostly is associated with, in the database. -----/////////


if ($ReferenceCulture == $Culture){ 
  return false;
}
  else {
    return true;
  }
 
