/////////----- Checks if Material has the value that the cointype mostly is associated with, in the database. -----/////////


if ($ReferenceMaterial == $Material){ 
  return false;
}
  else {
    return true;
  }
 
