/////////----- Checks if Denomination has the value that the cointype mostly is associated with, in the database. -----/////////


if ($ReferenceDenomination == $Denomination){ 
  return false;
}
  else {
    return true;
  }
 
