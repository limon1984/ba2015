/////////----- Checks if DateFrom/To has the value that the cointype mostly is associated with, in the database. -----/////////


if (($ReferenceDateFrom == $DateFrom) && ($ReferenceDateTo == $DateTo)){ 
  return false;
}
  else {
    return true;
  }
 
