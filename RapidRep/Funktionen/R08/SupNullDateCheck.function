/////////----- Checks if char is set instead of NULL. -----/////////


if ($DateWritten == '?' || $DateWritten == '#' || $DateWritten == '-' || $DateWritten == '. ?'){ 
  return true;
}
  else {
    return false;
  }
 
