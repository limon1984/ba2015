/////////----- Checks if char is set instead of NULL. -----/////////


if ($YearFound == '?' || $YearFound == '#' || $YearFound == '-' || $YearFound == '. ?'){ 
  return true;
}
  else {
    return false;
  }
 
